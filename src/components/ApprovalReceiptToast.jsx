import React from 'react';
export default function ApprovalReceiptToast({receipt,onView,onUndo,onClose}){if(!receipt)return null;return <div className="approval-receipt-toast" role="status"><div><strong>{receipt.status === 'approved' ? 'Approval recorded' : receipt.status === 'rejected' ? 'Rejection recorded' : 'Card placed on hold'}</strong><p>Receipt {receipt.receiptId}. {receipt.nextStep}</p><small>Underlying action executed: no · execution remains gated</small></div><div><button type="button" onClick={onView}>View receipt</button><button type="button" onClick={onUndo}>Undo local status</button><button type="button" onClick={onClose}>Dismiss</button></div></div>}
