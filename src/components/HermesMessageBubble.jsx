import React from 'react';
export default function HermesMessageBubble({ message, onDelegate, onReview, onSpecialist }) { return <div className={`nxos-message ${message.role}`}><strong>{message.role === 'ray' ? 'Ray' : 'Hermes'}</strong><p>{message.text}</p><div className="hermes-message-actions">{message.role === 'ray' && <button type="button" onClick={() => onDelegate(message)}>Delegate this</button>}{message.role === 'hermes' && <><button type="button" onClick={() => onReview(message)}>Create Ray Review card</button><button type="button" onClick={() => onSpecialist(message)}>Send to specialist</button></>}</div></div>; }
