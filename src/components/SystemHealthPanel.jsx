import React,{useState} from 'react';import {systemHealthItems} from '../data/systemHealthData';import SystemHealthDetailDrawer from './SystemHealthDetailDrawer';
export default function SystemHealthPanel({onNavigate,onAskHermes}){const [selected,setSelected]=useState(null);return <div className="system-health-layout"><section className="health-grid">{systemHealthItems.map(item=><button type="button" className={`health-item health-${item.tone}`} key={item.id} onClick={()=>setSelected(item)}><span className="health-dot"/><span><strong>{item.label}</strong><small>{item.status}</small></span><i>Details →</i></button>)}</section><SystemHealthDetailDrawer item={selected} onClose={()=>setSelected(null)} onNavigate={onNavigate} onAskHermes={onAskHermes}/>{!selected&&<section className="glass health-empty-detail"><span>◎</span><h3>Select any system</h3><p>Every health row opens status, evidence, next action, and copy controls.</p></section>}</div>}
