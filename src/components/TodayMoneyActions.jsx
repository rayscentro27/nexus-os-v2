import React from 'react';
export default function TodayMoneyActions({onNavigate}){const actions=[['Prove the $97 readiness journey','Approve the synthetic insert, verify the dashboard, then complete Stripe test Checkout.','rayreview'],['Clear the communication gate','Fix Resend domain/key scope; keep every message as a draft until approved.','reports'],['Choose the first revenue candidates','Review the strongest items from the 26 immediately actionable research opportunities.','opportunity']];return <section className="glass panel nexus-money-actions"><div className="section-title"><div><h3>Today’s money actions</h3><p>Move proof and revenue—not more setup</p></div><span className="pill pill-green">Priority</span></div>{actions.map(([title,detail,target],index)=><button type="button" key={title} onClick={()=>onNavigate(target)}><b>{index+1}</b><span><strong>{title}</strong><small>{detail}</small></span><i>Open →</i></button>)}</section>}
