import React,{useState} from 'react'; import {buildHermesResponse} from '../data/hermesWorkroomData';
export default function HermesQuickPanel({onOpen}){const [input,setInput]=useState('');const [answer,setAnswer]=useState('Good morning, Ray. I’m watching the money path, approval queue, and operating blockers. What do you want to move today?');function ask(){if(!input.trim())return;setAnswer(buildHermesResponse(input).text);setInput('')}return <section className="glass hermes-quick-panel"><header><div className="hermes-orb">✦</div><div><h3>Hermes</h3><p>Ray’s CEO Advisor · Context active</p></div><span className="pill pill-green">Advisor mode</span></header><div className="hermes-quick-answer">{answer}</div><div className="hermes-quick-input"><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')ask()}} placeholder="Talk to Hermes…"/><button type="button" onClick={ask}>Ask</button></div><button type="button" className="open-workroom" onClick={onOpen}>Open the Hermes Workroom →</button></section>}
