import React from 'react';
const cards=[['operating_cycles','Operating cycles','2 loaded','08:00 daily · 18:00 closeout','green'],['ray_review','Ray Review','64 cards','12 priority decisions','amber'],['research_money','Research to money','50 candidates','26 immediately actionable','blue'],['revenue','Confirmed revenue','$0','$97 test journey is next','violet']];
export default function OperatingStatusCards({onAction}){return <div className="metrics-grid nexus-home-metrics">{cards.map(([id,label,value,detail,tone])=><button type="button" className={`glass nexus-home-metric tone-${tone}`} key={id} onClick={()=>onAction(id)}><span>{label}</span><strong>{value}</strong><small>{detail}</small><i>Open →</i></button>)}</div>}
