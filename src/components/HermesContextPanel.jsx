import React from 'react'; import { hermesContext } from '../data/hermesContextData';
export default function HermesContextPanel() { return <aside className="hermes-context-panel glass"><div className="section-title"><div><h3>Operating context</h3><p>What I’m watching for you</p></div></div>{[['Running safely',hermesContext.runningSafely],['Approvals needed',hermesContext.approvalsNeeded],['Money actions',hermesContext.moneyActions],['Blockers',hermesContext.blockers]].map(([title,items])=><section key={title}><strong>{title}</strong>{items.map(item=><span key={item}>{item}</span>)}</section>)}</aside>; }
