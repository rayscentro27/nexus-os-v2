export const ALPHA_MODES = ["General Conversation","Business Ideas","Startup Scout","SEO / Money Opportunities","Affiliate Strategy","Marketing Drafts","Voice Agent Ideas","App Builder Ideas","Trading Research","Tool / Repo Review","Ray Review Draft","Handoff Mode"] as const;
export type AlphaConversationMode = typeof ALPHA_MODES[number];
export function inferAlphaMode(prompt:string, selected:AlphaConversationMode="General Conversation"):AlphaConversationMode { const p=prompt.toLowerCase(); if(/handoff/.test(p))return "Handoff Mode"; if(/ray review/.test(p))return "Ray Review Draft"; if(/soccer|change the subject/.test(p))return "General Conversation"; if(/voice agent/.test(p))return "Voice Agent Ideas"; if(/build an app|app around|app idea/.test(p))return "App Builder Ideas"; if(/trade|trading|forex|backtest/.test(p))return "Trading Research"; if(/affiliate/.test(p))return "Affiliate Strategy"; if(/landing page|marketing/.test(p))return "Marketing Drafts"; if(/seo|money opportun/.test(p))return "SEO / Money Opportunities"; if(/startup|low-cost/.test(p))return "Startup Scout"; if(/repo|tool/.test(p))return "Tool / Repo Review"; if(/business idea|idea for selling/.test(p))return "Business Ideas"; return selected; }
