import { affiliatePlaceholders, getConnectorRegistry } from "./nexusConnectorRegistry";
export function getAffiliateApiSetupCenter(){const affiliates=[...affiliatePlaceholders,{id:'voice_agent_tools',value:'',status:'missing_account' as const,approvalRequired:true}];return {label:"INTERNAL OPERATIONS — DRAFT ONLY",connectors:getConnectorRegistry(),affiliates:affiliates.map(a=>({...a,purpose:a.id.replaceAll("_"," "),required:false,inputLocation:"Nexus Operations > Affiliate/API Setup Center",safety:"approval_gated",featureUnlocked:"approved referral draft",clientFacingRisk:"high",notes:"Enter an approved URL later; no fake link is supplied."})),secretsDisplayed:false,externalActivationPerformed:false};}
