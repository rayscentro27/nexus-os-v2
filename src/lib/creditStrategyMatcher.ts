import type { BureauDiscrepancy, CanonicalAccount } from './crossBureauCreditComparison'
import { CREDIT_STRATEGY_CATALOG, type CreditStrategyDefinition } from './creditStrategyCatalog'
import { toolsForStrategy } from './creditStrategyToolMatrix'

export interface MatchedCreditStrategy { recommendationId:string; canonicalAccountId:string; discrepancyId:string|null; primaryStrategy:CreditStrategyDefinition; alternativeStrategies:CreditStrategyDefinition[]; clientConfirmationQuestions:string[]; requiredEvidence:string[]; availableTools:string[]; confidence:'high'|'medium'|'low'; specialistException:boolean; rationale:string }
export function matchCreditStrategies(input:{canonicalAccounts:CanonicalAccount[];discrepancies:BureauDiscrepancy[];catalog?:CreditStrategyDefinition[]}):MatchedCreditStrategy[]{const catalog=input.catalog||CREDIT_STRATEGY_CATALOG; return input.discrepancies.flatMap(d=>{const matches=catalog.filter(s=>s.categories.some(c=>d.possibleStrategyCategories.includes(c)||c===d.discrepancyType)); if(!matches.length)return []; const account=input.canonicalAccounts.find(a=>a.canonicalAccountId===d.canonicalAccountId); const sorted=[...matches].sort((a,b)=>Number(b.approvalStatus==='approved_for_tool_use')-Number(a.approvalStatus==='approved_for_tool_use')); const primary=sorted[0]; const tool=toolsForStrategy(d.discrepancyType==='date_opened_mismatch'||d.discrepancyType==='last_reported_date_mismatch'?'date_mismatch':d.discrepancyType); return [{recommendationId:`rec_${d.discrepancyId}_${primary.strategyId}`,canonicalAccountId:d.canonicalAccountId,discrepancyId:d.discrepancyId,primaryStrategy:primary,alternativeStrategies:sorted.slice(1,4),clientConfirmationQuestions:primary.clientFactsNeeded,requiredEvidence:primary.requiredEvidence,availableTools:[...new Set([...primary.availableTools,...tool.tools])],confidence:d.confidence,specialistException:d.specialistExceptionRequired||account?.matchConfidence==='low',rationale:`Objective ${d.discrepancyType.replaceAll('_',' ')} matched to version ${primary.version} of an approved reusable strategy.`}]})}
