export type FinancialValue = { value: number | null; currency: string; asOf?: string; source: string; classification: 'OBSERVED_REAL' | 'IMPORTED' | 'MODELED' | 'UNKNOWN'; confidence: 'HIGH' | 'MEDIUM' | 'LOW' | 'NONE' }
export type Reserve = { category: string; targetAmount: FinancialValue; currentAmount: FinancialValue; shortfall: number | null; status: 'ON_TARGET' | 'SHORTFALL' | 'UNKNOWN'; rationale: string; evidence: string[]; approvalRequired: boolean }
export function runway(cash: FinancialValue, burn: FinancialValue) { if (cash.value === null || burn.value === null || burn.value <= 0) return { months: null, precision: 'UNKNOWN' as const }; return { months: Math.round((cash.value / burn.value) * 10) / 10, precision: cash.classification === 'OBSERVED_REAL' && burn.classification === 'OBSERVED_REAL' ? 'OBSERVED_INPUTS' as const : 'MODELED_INPUTS' as const } }
export function allocationRecommendation(category: string, percent: number, rationale: string) { return { category, percent, rationale, confidence: 'LOW' as const, approvalRequired: true, executionStatus: 'RECOMMEND' as const, externalTransaction: false } }
export function transactionGate(action: string, approved: boolean) { return { action, allowed: false, outcome: approved ? 'PREPARE_FOR_APPROVAL' : 'RECOMMEND', approvalRequired: true, executed: false, reason: 'Real financial transactions require fresh explicit approval.' } as const }
export function scenario(name: 'CONSERVATIVE' | 'BASE' | 'GROWTH' | 'STRESS', inputs: { revenue: number; expenses: number; reserves: number }) { return { name, modeled: true, net: inputs.revenue - inputs.expenses - inputs.reserves, inputs } }
