export type OpportunityStatus = 'REAL_OPPORTUNITY' | 'PROMISING_BUT_UNVALIDATED' | 'EXPERIMENT_REQUIRED' | 'LOW_CONFIDENCE' | 'WEAK_ECONOMICS' | 'HYPE' | 'STALE' | 'REJECTED'
export type OpportunityInput = { sourceQuality: number; recency: number; provenance: number; alpha: number; marketSignal: number; customerRelevance: number; economicSupport: number; implementationCost: number; timeToTest: number; risk: number; differentiation: number; hype: number; stale: boolean; contradicted: boolean }
export function scoreOpportunity(i: OpportunityInput) { const score = Math.round(100 * (.16*i.sourceQuality + .12*i.recency + .14*i.provenance + .14*i.alpha + .1*i.marketSignal + .1*i.customerRelevance + .12*i.economicSupport + .08*(1-i.implementationCost) + .04*(1-i.timeToTest) + .04*i.differentiation - .08*i.risk - .16*i.hype)); const status: OpportunityStatus = i.stale ? 'STALE' : i.contradicted ? 'REJECTED' : i.hype > .7 && i.provenance < .5 ? 'HYPE' : i.economicSupport < .35 ? 'WEAK_ECONOMICS' : score >= 65 ? 'EXPERIMENT_REQUIRED' : 'PROMISING_BUT_UNVALIDATED'; return { score: Math.max(0, Math.min(100, score)), status, experimentRequired: status === 'EXPERIMENT_REQUIRED' || status === 'PROMISING_BUT_UNVALIDATED' } }
export function buildExperiment(opportunityId: string, ownerDepartment: string) { return { experimentId: `experiment-${opportunityId}`, opportunityId, ownerDepartment, status: 'READY_FOR_GOVERNED_VALIDATION', budgetLimit: 0, timeLimit: 'bounded', riskLimit: 'low', externalSideEffect: false, successCriterion: 'Evidence-backed signal improves confidence without unsupported economics', failureCriterion: 'No signal or material evidence contradiction', requiredApproval: true } }
export function buildHandoff(opportunityId: string, department: string, evaluation: ReturnType<typeof scoreOpportunity>) { return { opportunityId, targetDepartment: department, score: evaluation.score, status: evaluation.status, evidence: 'source and provenance references required', alphaResult: 'challenge recorded', economicStatus: evaluation.status === 'WEAK_ECONOMICS' ? 'UNVALIDATED' : 'MODELED_OR_UNVALIDATED', knownGaps: ['direct demand', 'CAC', 'conversion', 'retention'], experimentRequired: evaluation.experimentRequired } }
