export type EconomicClassification = 'OBSERVED_FACT' | 'SUPPORTED_ESTIMATE' | 'HYPOTHESIS' | 'ASSUMPTION' | 'VALIDATED_RESULT' | 'UNVALIDATED_RESULT'
export type EconomicValue = { value: number | null; classification: EconomicClassification; source: string; evidenceId?: string; confidence: 'HIGH' | 'MEDIUM' | 'LOW' | 'NONE' }
export type PricingHypothesis = { hypothesisId: string; offer: string; proposedPrice: number; baselinePrice: number | null; expectedValue: string; expectedCustomer: string; rationale: string; evidence: string[]; risks: string[]; validationMethod: string; successCriterion: string; failureCriterion: string; status: 'UNVALIDATED_HYPOTHESIS' | 'VALIDATED_RESULT' | 'REJECTED' }
export function unitEconomics(input: { price: EconomicValue; units: EconomicValue; serviceCost: EconomicValue; computeCost: EconomicValue; paymentCost: EconomicValue; fulfillmentCost: EconomicValue; cac: EconomicValue; retention: EconomicValue }) { const revenue = input.price.value !== null && input.units.value !== null ? input.price.value * input.units.value : null; const costs = [input.serviceCost, input.computeCost, input.paymentCost, input.fulfillmentCost].every((v) => v.value !== null) ? [input.serviceCost, input.computeCost, input.paymentCost, input.fulfillmentCost].reduce((sum, v) => sum + (v.value || 0), 0) * (input.units.value || 0) : null; return { revenue, costs, grossMargin: revenue !== null && costs !== null ? revenue - costs : null, contributionMargin: revenue !== null && costs !== null && input.cac.value !== null ? revenue - costs - (input.cac.value * (input.units.value || 0)) : null, breakevenCustomers: null, ltv: null, paybackPeriod: null } }
export function scenario(name: 'LOW_CASE' | 'BASE_CASE' | 'HIGH_CASE', a: { customers: number; conversionRate: number; price: number; serviceCost: number; computeCost: number; paymentCost: number; fulfillmentCost: number; cac: number }) { const customersConverted = a.customers * a.conversionRate; const revenue = customersConverted * a.price; const variableCost = customersConverted * (a.serviceCost + a.computeCost + a.paymentCost + a.fulfillmentCost); return { name, modeled: true, assumptions: a, customersConverted, revenue, contribution: revenue - variableCost - (customersConverted * a.cac) } }
export function challengeEconomics(claims: Array<{ claim: string; classification: EconomicClassification; evidence?: string }>) { return claims.filter((c) => ['HYPOTHESIS', 'ASSUMPTION', 'UNVALIDATED_RESULT'].includes(c.classification) && !c.evidence).map((c) => ({ claim: c.claim, decision: 'REJECTED_UNSUPPORTED' })) }
