import type { BureauDiscrepancy, CanonicalAccount } from './crossBureauCreditComparison'
import type { MatchedCreditStrategy } from './creditStrategyMatcher'

export interface ClydeStrategyCard {id:string;reportItem:string;bureauComparison:string;detectedIssue:string;whyItMatters:string;primaryStrategy:string;alternativeStrategies:string[];researchConfidence:string;steps:string[];documents:string[];possibleOutcomes:string;limitations:string;tier1Impact:string;tier2Impact:string;availableTools:string[];clientQuestions:string[];actions:string[];requiresGoClearException:boolean}
export function createClydeStrategyCards(accounts:CanonicalAccount[],discrepancies:BureauDiscrepancy[],matches:MatchedCreditStrategy[]):ClydeStrategyCard[]{return matches.map(m=>{const d=discrepancies.find(x=>x.discrepancyId===m.discrepancyId)!; const a=accounts.find(x=>x.canonicalAccountId===m.canonicalAccountId); const values=Object.entries(d.bureauValues).map(([b,v])=>`${b}: ${typeof v==='number'?new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(v):String(v)}`).join(' · '); return {id:m.recommendationId,reportItem:`${a?.furnisher||'Report item'} ${a?.maskedAccountReference||''}`.trim(),bureauComparison:values,detectedIssue:d.differenceSummary,whyItMatters:d.fundingImpact,primaryStrategy:m.primaryStrategy.name,alternativeStrategies:m.alternativeStrategies.map(s=>s.name),researchConfidence:m.confidence,steps:['Review the objective comparison','Choose an approved option that matches your records','Upload relevant evidence when available','Review and authorize any draft before use'],documents:m.requiredEvidence,possibleOutcomes:m.primaryStrategy.possibleOutcomes,limitations:'Nexus cannot guarantee a correction, removal, score change, or funding decision.',tier1Impact:d.tier1Impact,tier2Impact:d.tier2Impact,availableTools:m.availableTools,clientQuestions:m.clientConfirmationQuestions,actions:['Use Recommended Strategy','Compare Other Options','Upload Evidence','Prepare Draft','Save for Later','Ask Clyde','Request GoClear Exception Review'],requiresGoClearException:m.specialistException}})}
