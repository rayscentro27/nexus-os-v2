import { describe, expect, it } from 'vitest'
import { buildBusinessPlanContext, evidence, readinessFromEvidence, type CustomerGoal, type UseOfFundsItem } from '../src/lib/customerGoalsModel'
const goal: CustomerGoal = { goalId: 'goal-test', customerOrClientId: 'client-test', goalType: 'BUSINESS_READINESS', title: 'Test goal', description: 'Test', targetOutcome: 'Review', priority: 1, currentStatus: 'IN_PROGRESS', currentProgress: 50, milestones: [{ milestoneId: 'm1', title: 'Provide evidence', status: 'IN_PROGRESS', completionState: 'OPEN', evidence: evidence('fixture', 'SUPPORTED', 'e1'), dependencies: [], nextAction: 'Upload evidence' }], dependencies: [], blockers: ['proof'], evidenceRequirements: ['proof'], evidence: evidence('fixture', 'PARTIAL'), createdAt: '2026-01-01', updatedAt: '2026-01-01' }
const funds = (status: 'SUPPORTED' | 'PARTIAL' | 'MISSING'): UseOfFundsItem => ({ useOfFundsId: 'u1', category: 'equipment', description: 'Item', businessPurpose: 'Business', relatedGoal: goal.goalId, supportingEvidence: evidence('fixture', status, status === 'SUPPORTED' ? 'e2' : undefined), evidenceStatus: status, confidence: status === 'SUPPORTED' ? 'HIGH' : status === 'PARTIAL' ? 'MEDIUM' : 'NONE', knownGaps: status === 'SUPPORTED' ? [] : ['proof'] })
describe('customer goals and business-plan contract', () => { it('binds goals, milestones, use-of-funds, and next action', () => { const context = buildBusinessPlanContext(goal, [funds('SUPPORTED')]); expect(context.goal.id).toBe(goal.goalId); expect(context.milestones).toHaveLength(1); expect(context.useOfFunds[0].evidenceStatus).toBe('SUPPORTED'); expect(context.recommendedNextAction).toBe('Upload evidence') }); it('keeps partial and missing evidence unverified', () => { expect(readinessFromEvidence(goal, [funds('PARTIAL')]).status).toBe('UNVERIFIED'); expect(readinessFromEvidence(goal, [funds('MISSING')]).unsupportedPositiveClaims).toBe(1) }) })
