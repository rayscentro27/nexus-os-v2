import {describe,it,expect} from 'vitest'; import {getAffiliateApiSetupCenter} from '../src/hermes/nexus/affiliateApiSetupCenter';
describe('setup center',()=>{it('contains blank placeholders and no secrets',()=>{const x=getAffiliateApiSetupCenter();expect(x.affiliates.length).toBe(10);expect(x.affiliates.every(a=>a.value===''&&a.approvalRequired)).toBe(true);expect(x.secretsDisplayed).toBe(false);expect(x.externalActivationPerformed).toBe(false)})});
