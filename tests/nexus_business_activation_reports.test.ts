import {describe,it,expect} from 'vitest';import fs from 'node:fs';
describe('business activation evidence',()=>{it('contains repo scout and activation reports',()=>{for(const p of ['reports/activation/open_source_repo_scout_for_alpha_and_nexus.md','reports/activation/open_source_repo_priority_matrix.md','reports/activation/open_source_repo_do_not_install_yet.md','reports/activation/nexus_business_activation_latest.json'])expect(fs.existsSync(p)).toBe(true)});it('records no prohibited action',()=>{const x=JSON.parse(fs.readFileSync('reports/activation/nexus_business_activation_latest.json','utf8'));expect(x).toMatchObject({external_actions:0,supabase_writes:0,emails_sent:0,social_posts:0,charges:0,orders:0,real_client_records:0,safety:'passed'})})});
