import { describe, expect, it } from 'vitest'
import { notebookIndex, safeClaimStatus, type NotebookClaim, type NotebookContradiction } from '../src/lib/researchNotebook'
const claim: NotebookClaim = { claimId: 'c1', statement: 'Bounded claim', topic: 'test', status: 'SUPPORTED', confidence: 'HIGH', sourceIds: ['s1'], supportingEvidence: ['e1'], contradictingEvidence: [], alphaStatus: 'PENDING', departmentRelevance: ['Research'] }
const conflict: NotebookContradiction = { contradictionId: 'x1', claimId: 'c1', sourceA: 's1', sourceB: 's2', description: 'Conflict', severity: 'HIGH', resolutionStatus: 'UNRESOLVED', alphaReviewRequired: true }
describe('governed research notebook', () => { it('downgrades unresolved contradictions', () => { expect(safeClaimStatus(claim, conflict)).toEqual({ status: 'CONTRADICTED', confidence: 'LOW' }); expect(safeClaimStatus({ ...claim, sourceIds: [], supportingEvidence: [] })).toEqual({ status: 'UNVERIFIED', confidence: 'NONE' }) }); it('indexes claims and source freshness', () => { const index = notebookIndex([{ sourceId: 's1', sourceType: 'PUBLIC_WEB', location: 'https://example.com', title: 'Example', discoveredAt: '2026-01-01', fetchStatus: 'STALE', provenance: 'bounded read', quality: 'MEDIUM', primary: false, departmentRelevance: ['Research'], tags: ['test'] }], [claim], [conflict]); expect(index.currentSupported).toHaveLength(1); expect(index.staleSources).toHaveLength(1); expect(index.contradictions).toHaveLength(1) }) })
