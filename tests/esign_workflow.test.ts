import { describe, expect, it } from 'vitest'
import { canSignatureTransition, prepareSignatureRequest, resolveActiveTemplate, type DocumentInstance, type DocumentTemplate } from '../src/lib/esignWorkflow'
const template: DocumentTemplate = { templateId: 't1', templateName: 'Test', documentType: 'service_agreement', version: 2, status: 'ACTIVE', effectiveAt: '2026-01-01', contentHash: 'hash-v2', requiredFields: ['name'], requiredSignatures: ['client'], requiredConsents: ['terms-v2'], retentionClass: 'POLICY_DEFINED', source: 'approved template' }
const instance: DocumentInstance = { documentInstanceId: 'd1', templateId: 't1', templateVersion: 2, clientOrBusinessId: 'synthetic', workflowId: 'w1', contentHash: 'hash-v2', sourceDataReferences: ['profile'], signatureStatus: 'READY', consentStatus: 'VALID', retentionStatus: 'RETAIN', auditHistory: [] }
describe('governed e-sign workflow', () => { it('resolves active versions and preserves traceability', () => { expect(resolveActiveTemplate([template, { ...template, version: 1, status: 'SUPERSEDED' }], 't1')?.version).toBe(2); expect(instance.templateVersion).toBe(template.version); expect(instance.contentHash).toBe(template.contentHash) }); it('requires provider evidence for signed and consent for requests', () => { expect(canSignatureTransition('VIEWED', 'SIGNED', false)).toBe(false); expect(canSignatureTransition('VIEWED', 'SIGNED', true)).toBe(true); expect(prepareSignatureRequest(instance, true, 'VALID')).toMatchObject({ ready: true, approvalRequired: true, externalRequestPerformed: false }); expect(prepareSignatureRequest(instance, true, 'WITHDRAWN').ready).toBe(false) }) })
