import {describe,it,expect} from 'vitest'; import fs from 'node:fs';
describe('sprint safety guards',()=>{it('keeps Alpha free of Supabase imports',()=>{for(const p of ['src/hermes/alpha/hermesAlphaOpportunityBrain.ts','src/hermes/alpha/alphaSeoMoneyOpportunityEngine.ts','src/hermes/alpha/alphaTradingResearchPipeline.ts'])expect(fs.readFileSync(p,'utf8')).not.toMatch(/from ["'][^"']*supabase/i)});it('keeps UI risks disabled',()=>{const s=fs.readFileSync('src/components/NexusOperationsPanel.jsx','utf8');for(const x of ['Send','Publish','Charge','Trade live','Use real client data'])expect(s).toContain(x)})});
