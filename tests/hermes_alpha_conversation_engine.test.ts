import {describe,it,expect} from 'vitest';import {respondAsAlpha,alphaProviderStatuses} from '../src/hermes/alpha/hermesAlphaConversationEngine';
describe('Alpha conversation engine',()=>{it.each([['I have an idea for selling voice agents. Is it realistic?','real opportunity'],['Give me 3 low-cost startup ideas.','validation ideas'],['Can we build an app around this?','proven the workflow manually'],['Give me a landing page draft.','Headline'],['Give me an affiliate strategy.','problem-first education'],['What should I test first?','Got Funding interest page'],['Can this trading strategy work?','researchable, not yet proven'],['I want to change the subject.','Sure'],['Prepare a handoff prompt.','Handoff draft'],['Create a Ray Review draft.','Ray Review draft']])('answers %s',(q,part)=>expect(respondAsAlpha(q).text).toContain(part));it('greets Ray naturally with time awareness',()=>{const morning=respondAsAlpha('good morning',undefined,new Date('2026-07-04T08:00:00').getTime());expect(morning.text).toMatch(/Good morning/);const afternoon=respondAsAlpha('good afternoon',undefined,new Date('2026-07-04T14:00:00').getTime());expect(afternoon.text).toMatch(/Good afternoon/);const evening=respondAsAlpha('good evening',undefined,new Date('2026-07-04T20:00:00').getTime());expect(evening.text).toMatch(/Good evening/)});it('handles casual greetings naturally',()=>expect(respondAsAlpha('hello there').text).toContain("What's on your mind"));it('explains capabilities plainly',()=>expect(respondAsAlpha('what can you answer?').text).toMatch(/business ideas.*startup experiments.*affiliate.*SEO.*voice-agent/s));it('discloses unavailable live sports lookup',()=>expect(respondAsAlpha('What happened in the soccer match?').text).toContain("can't look up live scores"));it('never uses Supabase/client data/action',()=>expect(respondAsAlpha('What money opportunities does Nexus have right now?')).toMatchObject({brain:'Hermes Alpha',noSupabaseUsed:true,clientDataUsed:false,externalActionPerformed:false,provider:'deterministic_local'}));it('truthfully keeps deterministic fallback',()=>expect(alphaProviderStatuses.find(x=>x.available)?.provider).toBe('deterministic_local'))});
