import {beforeEach,describe,it,expect} from 'vitest';import {getAlphaUsageSummary,providerAllowedForMode,recordDeepBlocked,recordFallback,recordHosted,recordLocal,recordSearch,MAX_HOSTED_CALLS_FALLBACK,MAX_SEARCH_RESULTS} from '../src/hermes/alpha/alphaCostController';
const store=new Map<string,string>();beforeEach(()=>{store.clear();Object.defineProperty(globalThis,'localStorage',{value:{getItem:(k:string)=>store.get(k)||null,setItem:(k:string,v:string)=>store.set(k,v),removeItem:(k:string)=>store.delete(k)},configurable:true})});
describe('Alpha cost controller',()=>{it('tracks local, hosted, search, tokens, fallback and cache',()=>{recordLocal();recordHosted('groq',120,40,.001);recordSearch();recordSearch(true);recordFallback();const x=getAlphaUsageSummary();expect(x).toMatchObject({localCalls:1,hostedCalls:1,searchCalls:1,estimatedInputTokens:120,estimatedOutputTokens:40,fallbackCount:1,cacheHits:1,maxHosted:MAX_HOSTED_CALLS_FALLBACK});expect(x.estimatedSpendLabel).toContain('estimated')});it('locks deep mode and enforces cost modes',()=>{recordDeepBlocked();expect(getAlphaUsageSummary().deepModeUnlocked).toBe(false);expect(getAlphaUsageSummary().blockedDeepCalls).toBe(1);expect(providerAllowedForMode('groq','cheap')).toBe(false);expect(providerAllowedForMode('groq','fast')).toBe(true);expect(providerAllowedForMode('openrouter','strategy')).toBe(true)});it('caps search results at five',()=>expect(MAX_SEARCH_RESULTS).toBe(5))});
