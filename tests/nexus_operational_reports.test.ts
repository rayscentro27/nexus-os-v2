import {describe,it,expect} from 'vitest'; import fs from 'node:fs';
describe('operational evidence',()=>{it('has blocker and required report inventory',()=>{for(const p of ['reports/operations/nexus_full_automation_blockers.md','reports/operations/nexus_required_reports_inventory.md','reports/operations/nexus_operational_readiness_scorecard.md'])expect(fs.existsSync(p)).toBe(true)});it('records a zero-action end-to-end cycle',()=>{const x=JSON.parse(fs.readFileSync('reports/operations/nexus_operational_cycle_latest.json','utf8'));expect(x).toMatchObject({status:'passed',external_actions:0,production_writes:0,real_client_records:0,live_trades:0})})});
