import {describe,it,expect} from 'vitest';import fs from 'node:fs';
describe('Alpha workspace UI',()=>{it('is full direct workspace with required controls',()=>{const s=fs.readFileSync('src/components/HermesAlphaWorkspace.tsx','utf8');for(const x of ['Conversation','Idea / Opportunity Canvas','Sources & Providers','Action Draft','Local Session Memory','Handoff / export','Clear conversation','Send to Alpha'])expect(s).toContain(x);expect(s).not.toMatch(/import .*hermes\/nexus/i)});it('offers all modes',()=>{const s=fs.readFileSync('src/hermes/alpha/hermesAlphaModeRouter.ts','utf8');expect((s.match(/"[A-Za-z /]+"/g)||[]).length).toBeGreaterThanOrEqual(12)})});
