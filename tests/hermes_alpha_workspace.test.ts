import {describe,it,expect} from 'vitest';import fs from 'node:fs';
describe('Alpha workspace UI',()=>{it('is direct and shows provider, memory, cost, search, and safety controls',()=>{const s=fs.readFileSync('src/components/HermesAlphaWorkspace.tsx','utf8');for(const x of ['Conversation','Opportunity Canvas','Provider & Cost Controls','Usage Today','Memory & Sources','Action Draft','Handoff / export','Clear conversation','Send to Alpha','ALPHA IS SEPARATE FROM NEXUS HERMES','No Supabase','No client data','alpha-sticky-composer','position:\'sticky\''])expect(s).toContain(x);expect(s).not.toMatch(/import .*hermes\/nexus/i)});it('offers all modes',()=>{const s=fs.readFileSync('src/hermes/alpha/hermesAlphaModeRouter.ts','utf8');expect((s.match(/"[A-Za-z /]+"/g)||[]).length).toBeGreaterThanOrEqual(12)})});
