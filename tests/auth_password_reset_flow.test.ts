import {describe,it,expect} from 'vitest';import fs from 'node:fs';
describe('Nexus password reset flow',()=>{const source=fs.readFileSync('src/components/auth.tsx','utf8');it('offers forgot-password request through Supabase auth',()=>{expect(source).toContain('Forgot password?');expect(source).toContain('resetPasswordForEmail');expect(source).toContain("password-recovery=1")});it('sets password only in authenticated recovery session',()=>{expect(source).toContain("event === 'PASSWORD_RECOVERY'");expect(source).toContain('updateUser({password})');expect(source).toContain('minLength={12}')});it('does not generate or write password files',()=>expect(source).not.toMatch(/writeFile|localStorage.*password|\/tmp\/|admin_new_password/))});
