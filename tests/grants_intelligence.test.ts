import { describe, expect, it } from 'vitest'
import { buildGrantPackage, detectMissingInformation, matchEligibility, submissionGate, type GrantRecord } from '../src/lib/grantsIntelligence'
const grant: GrantRecord = { grantId: 'g1', title: 'Synthetic grant', sponsor: 'Primary sponsor', sourceUrl: 'https://example.com/grant', sourceType: 'PRIMARY_SOURCE', eligibilityRules: ['entity', 'revenue'], requiredDocuments: ['formation'], sourceFreshness: 'CURRENT', provenance: 'bounded primary-source read', confidence: 'HIGH', useOfFundsRestrictions: ['business use'] }
describe('governed grants intelligence', () => { it('keeps missing eligibility explicit and prepares context', () => { const m = matchEligibility(grant, { entity: 'VERIFIED_MATCH' }); expect(m.revenue).toBe('UNKNOWN'); expect(detectMissingInformation(grant, m)).toEqual(['revenue']); const p = buildGrantPackage(grant, m, ['business readiness'], ['equipment'], []); expect(p.submissionAllowed).toBe(false); expect(p.missingItems).toEqual(['revenue']) }); it('hard-blocks autonomous submission', () => { expect(submissionGate()).toMatchObject({ allowed: false, externalSubmissionPerformed: false, duplicateProtection: true }) }) })
