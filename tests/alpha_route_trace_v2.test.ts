import {describe,it,expect} from 'vitest';import {buildTrace,traceSummary} from '../src/hermes/alpha/alphaRouteTrace';
describe('Alpha route trace v2',()=>{it('records deterministic source, memory and hard data boundaries',()=>{const t=buildTrace({mode:'Business Ideas',memory_used:true,memory_source:'session_memory'});expect(t).toMatchObject({selected_provider:'deterministic_local',provider_used:'deterministic_local',provider_source:'deterministic_local',memory_used:true,memory_source:'session_memory',web_used:false,web_status:'disabled',supabase_used:false,client_data_used:false,estimated_hosted_call:false,estimated_search_call:false,estimated_cost_label:'local_free'});expect(t.message_id).toMatch(/^alpha-/);expect(t.timestamp_source).toBe('browser_local');expect(traceSummary(t)).toContain('No Supabase · No client data')});it('records backend provider and search sources',()=>{const t=buildTrace({selected_provider:'groq',provider_used:'groq',provider_source:'groq_backend',web_used:true,web_provider:'duckduckgo_keyless',web_status:'searched',web_query:'latest market',web_result_count:5,estimated_hosted_call:true,estimated_search_call:true,estimated_cost_label:'hosted_estimate'});expect(t.provider_used).toBe('groq');expect(t.web_result_count).toBeLessThanOrEqual(5);expect(traceSummary(t)).toMatch(/Groq backend.*duckduckgo/)});it('records honest fallback reason',()=>expect(buildTrace({selected_provider:'openrouter',provider_source:'unavailable_fallback',fallback_reason:'provider_unavailable'})).toMatchObject({fallback_reason:'provider_unavailable',supabase_used:false,client_data_used:false}))});
