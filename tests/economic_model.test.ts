import { describe, expect, it } from 'vitest'
import { challengeEconomics, scenario, unitEconomics, type EconomicValue } from '../src/lib/economicModel'
const unknown = (source: string): EconomicValue => ({ value: null, classification: 'ASSUMPTION', source, confidence: 'NONE' })
describe('governed GoClear economics', () => { it('preserves unknown inputs and does not fabricate margins', () => { const r = unitEconomics({ price: { value: 97, classification: 'OBSERVED_FACT', source: 'approved offer catalog' }, units: { value: 0, classification: 'OBSERVED_FACT', source: 'revenue state' }, serviceCost: unknown('no cost receipt'), computeCost: unknown('no cost receipt'), paymentCost: unknown('no processor receipt'), fulfillmentCost: unknown('no fulfillment receipt'), cac: unknown('no acquisition receipt'), retention: unknown('no retention receipt') }); expect(r.revenue).toBe(0); expect(r.grossMargin).toBeNull(); expect(r.ltv).toBeNull() }); it('labels scenarios as modeled, not forecasts', () => { const r = scenario('BASE_CASE', { customers: 10, conversionRate: .2, price: 97, serviceCost: 10, computeCost: 1, paymentCost: 3, fulfillmentCost: 20, cac: 5 }); expect(r.modeled).toBe(true); expect(r.revenue).toBe(194) }); it('rejects unsupported positive economics claims', () => { expect(challengeEconomics([{ claim: 'CAC is $20', classification: 'ASSUMPTION' }])).toHaveLength(1) }) })
