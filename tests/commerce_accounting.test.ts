import { describe, expect, it } from 'vitest'
import { canTransition, paymentState, summarizeReceivables, type Invoice } from '../src/lib/commerceAccounting'
const base = (status: Invoice['status']): Invoice => ({ invoiceId: `i-${status}`, customerId: 'synthetic', offerOrService: 'test', lineItems: [{ description: 'test', amount: 97 }], subtotal: 97, discount: 0, total: 97, amountPaid: status === 'PAID' ? 97 : 0, amountDue: status === 'PAID' ? 0 : 97, currency: 'USD', status, auditHistory: [] })
describe('commerce accounting controls', () => { it('enforces invoice lifecycle transitions', () => { expect(canTransition('DRAFT', 'READY_FOR_REVIEW')).toBe(true); expect(canTransition('PAID', 'DRAFT')).toBe(false); expect(canTransition('SENT', 'PAID')).toBe(false) }); it('excludes draft/void and protects payment state', () => { const s = summarizeReceivables([base('DRAFT'), base('OPEN'), base('PAST_DUE'), base('VOID'), base('PAID')]); expect(s.totalOutstanding).toBe(194); expect(s.totalPastDue).toBe(97); expect(paymentState(97, 97, false)).toBe('UNPAID'); expect(paymentState(97, 97, true)).toBe('PAID') }) })
