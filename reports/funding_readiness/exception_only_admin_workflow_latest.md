# Exception-Only Admin Workflow

The existing workbench emphasizes generated cards, decisions, exceptions, research approval work, and failed jobs. Confident matches are `client_choice_pending`; ambiguous matches are `exception_required` and hidden from clients.

Manual tools remain fallback. GoClear handles identity theft, legal/professional referral, complaint, conflicting response, and mailing failure cases.
