# Strategy Decision and Audit Foundation

Additive tenant-scoped tables persist versioned recommendations, append-only decisions, gated tool requests, and anonymized outcomes. Client RLS permits their visible recommendations and own decisions. Admin access uses `nexus_is_active_admin()`.

Outcome fields exclude raw values. Current language: “Outcome data is not yet sufficient.”
