# Alpha Real-World Evidence

## Run receipt

The live run persisted 42 source observations and 42 claims across:

- `https://github.com/mvanhorn/last30days-skill`
- `https://github.com/sushantkarn/SEO-engine`
- `@sharbelxyz`, `@Buildonaut-AI`, `@StedmanWaiters`, and `@Codacus` latest
  video lists, ten entries each.

Records are in governed `alpha_content` and `alpha_claims` JSONL stores. Each
record contains source ID/URL, retrieval time, content hash, evidence class,
claim, observation evidence, recency, independence, confidence, and explicit
unknowns. Model output is marked `model_output_not_evidence=true` and was not
used as source proof.

The source observations establish current source activity and titles; they do
not establish market success, revenue, SEO performance, or trading
profitability. All claims are therefore `UNVERIFIED` pending independent
verification. This is a real research result, not a fabricated winner.

The incremental rerun checked both repositories and the same four channels,
skipped 42 unchanged items, and exited successfully. Empty delta is now a
truthful completed heartbeat rather than a queue failure.
