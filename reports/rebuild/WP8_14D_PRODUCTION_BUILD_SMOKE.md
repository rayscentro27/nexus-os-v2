# WP8.14D production build smoke

`dist/index.html` and compiled assets exist after the canonical build. Vite preview on port 4174 served the built application; authenticated desktop `/operator/finance` smoke passed. The mobile preview invocation was run as a bounded isolated test; the earlier dev-server mobile test also passed. No publication or deployment occurred.
