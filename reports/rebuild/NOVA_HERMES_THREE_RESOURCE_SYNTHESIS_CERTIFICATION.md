# Nova Hermes three-resource synthesis certification

The three-resource path is structurally supported and exercised as:

`NEXUS_READ → PUBLIC_WEB_SEARCH → PUBLIC_WEB_RETRIEVAL → Nova synthesis`.

The observed multi-resource run returned real Nexus and web results in one
reasoning context and retrieved two pages. A full Nexus + web + fresh Alpha
follow-up remains a separate certification step because this campaign's
bounded probes did not claim completion of every asynchronous Alpha sequence.

`THREE_RESOURCE_SYNTHESIS=PARTIAL`, not a cutover pass.
