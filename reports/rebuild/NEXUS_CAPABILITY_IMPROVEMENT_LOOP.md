# Capability Improvement Loop

Defined: observe performance → detect gap → diagnose → research → propose
candidate → sandbox → benchmark → compare baseline → validate → promote or
reject → monitor → rollback. `improvement_candidate()` implements the version,
evidence, risk, status, and rollback contract. No production installation is
performed by this foundation.

