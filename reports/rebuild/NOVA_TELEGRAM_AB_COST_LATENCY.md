# Nova Telegram A/B cost and latency

The shadow receipt records primary and shadow latency, model, model-call count,
tool sequence, and bounded result payloads. Hermes uses the same baseline model
configuration (`openai/gpt-4o-mini`) unless the live environment reports an
explicit difference.

No paid provider was enabled and no new spend was incurred. Live cost/latency
comparison is `NOT_RUN` pending Ray's real prompts.
