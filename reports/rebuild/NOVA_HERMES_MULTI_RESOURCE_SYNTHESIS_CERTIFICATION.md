# Nova Hermes multi-resource synthesis certification

The request “Using Nexus and current outside information, what would you try
to make money in the next 30 days?” selected `nexus_read_shadow` and
`public_web_search_shadow`. With the retrieval guidance, the same native
Hermes turn also selected `public_web_retrieval_shadow` for two pages.

All tool results re-entered the same Hermes conversation before synthesis.
The final answer included a concrete opportunity direction, blockers, and next
actions. It was not a raw Nexus summary plus a raw web summary. Alpha was not
forced; it remains optional when Nova decides deeper challenge is useful.
