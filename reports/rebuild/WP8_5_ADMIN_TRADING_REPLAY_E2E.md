# Admin Trading Replay E2E

The React/Vite production build validates the new Admin Trading Lab route and component. The real WP8.5 experiment IDs and OANDA provenance are rendered, Tournament rows link to Replay, safety is visible, and replay controls are read-only. The current OOS records contain no fills, so the UI honestly shows no fill markers. A browser session against authenticated production data remains a Ray-facing verification step; no live order action exists in the surface.
