# Backtest vs Broker

The certification record provides the comparison boundary: requested one-unit market order, observed fill `1.15932`, close `1.15916`, Practice spread cost and realized P/L. It is deliberately excluded from strategy performance. A statistically useful backtest-to-broker model update requires multiple forward observations; no automatic model mutation occurs.
