# WP9K Skill Execution

The existing `system-operations` Nexus skill was copied into the dedicated
Oracle profile without installing new software. A natural current-state
prompt caused Hermes to use the skill and combine it with the live MCP read.
The result identified one queued approved work order waiting for an
engineering worker and distinguished safe Nexus handling from Ray attention.

This is execution evidence, not merely directory discovery:
`ORACLE_HERMES_SKILLS=PASS_REAL_EXECUTION`.
