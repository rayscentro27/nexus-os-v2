# WP9J MCP and skill execution

The local Nova shadow has MCP discovery and skill/tool bookkeeping, and the
Nexus MCP server is implemented. Those facts are not Oracle execution proof.

Oracle inspection showed the `nova_nexus` profile has an empty `mcp_servers`
map and no verified profile-specific skill execution receipt. The model route
was proven, but no Oracle tool call was made.

`ORACLE_HERMES_MCP=NOT_PROVEN_REAL_EXECUTION`
`ORACLE_HERMES_SKILLS=NOT_PROVEN_REAL_EXECUTION`

