# WP9L Delegation Evidence

`SPECIALIST_TOOL_CONTRACT=PASS`.

The model-driven flow completed after the wrapper was removed:

`Nova/Hermes 0.20.6 -> direct nexus_delegate_specialist -> Mac bearer boundary ->
SYSTEM canonical read -> hashed delegation receipt -> final synthesis`.

The bounded objective was current system health and a safe improvement. The
returned result was `DEGRADED` with partial telemetry and a stale operator
signal; the model did not claim an unverified repair.

`ORACLE_HERMES_DELEGATION=PASS_REAL`.
`HERMES_DELEGATION_FOLLOWUP=NOT_REQUIRED_WITH_EVIDENCE`; the specialist result
was sufficiently specific for the bounded probe, and no follow-up was
justified.
