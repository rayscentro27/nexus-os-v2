# OmniRoute Sandbox

No installation, listener, provider login, MCP, A2A, memory, or zero-cost route test was performed. This is intentional: the audit found no reason to introduce a second gateway before a scoped security review and explicit adapter test. `OMNIROUTE_PRODUCTION_ADOPTION=NO`; `OMNIROUTE_MCP_ENABLED=NO`; `OMNIROUTE_MEMORY_ENABLED=NO`; `OMNIROUTE_AGENT_LAYER_ENABLED=NO`.
