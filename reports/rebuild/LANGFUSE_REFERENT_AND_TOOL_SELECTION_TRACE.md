# Langfuse referent and tool-selection trace

Each generation records available resource classes, selected tool names,
selected resource type, and the current turn correlation. This makes the
opportunity-follow-up defect observable without changing referent resolution or
tool selection.
