# Oracle Hermes Runtime Certification — 2026-08-28

`CERTIFICATION_STATUS=NOT_RUN`

Certification is unavailable because the required Oracle container runtime is
absent and the host-install gate is pending. No Hermes version, health,
config, profile, provider, session, memory, reasoning, or restart claim is
made. Required post-gate checks are pinned runtime metadata, `/health`,
config/profile load, session create/resume, scoped memory, provider load, a
harmless bounded reasoning request, and restart persistence.

Routines, delegation, voice, and Active Operator remain disabled.
