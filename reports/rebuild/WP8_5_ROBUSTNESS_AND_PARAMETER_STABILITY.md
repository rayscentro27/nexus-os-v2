# Robustness and Parameter Stability

Each candidate received three bounded fast/slow perturbations and cost-aware evaluation. The zero-trade OOS sample cannot establish stability; this limitation is recorded rather than overstated. The contract is ready for larger history and walk-forward validation.
