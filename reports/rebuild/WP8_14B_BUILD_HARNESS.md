# WP8.14B Build harness

Focused Python Finance tests pass (`4 passed`). Direct `vite build` completes. The repository `npm run build` path has a pre-existing Tailwind/Vite process-hang behavior after Tailwind; it remains bounded as `NPM_FULL_BUILD_HARNESS=ROOT_CAUSE_BOUNDED_NOT_REPAIRED`. Direct Vite success is not used as npm-harness certification.
