# Restart Recovery Proof

The bounded daemon supports single-flight process operation and `--max-cycles`; state and seen signal identities persist across process boundaries. Startup runs account/position/pending-order reconciliation before evaluation. The real certification account was flat after close; an open-position restart was not performed because no position was left open after the required close proof.
