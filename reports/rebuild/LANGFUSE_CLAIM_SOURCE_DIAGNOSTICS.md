# Langfuse claim-source diagnostics

Material Nexus terms in final prose receive deterministic diagnostics without a
second model call. The diagnostic distinguishes fresh MCP from prior session
context and model inference, and marks unsupported Nexus claims when no fresh
MCP result exists. It is diagnostic metadata, not a truth store or response
rewriter.
