# WP9D authorization blocker proof

Figma was selected as a current Creative authorization blocker. Nexus prepared
the official OAuth URL, read/private-test-frame-write scopes, least-privilege
security review, zero-new-spend review, and durable resume step. Status is
`WAITING_HUMAN_CONSENT`; no account/session was discovered locally and no
authorization was bypassed.

Ray action, if desired: authorize the official Figma connection with the
listed scopes. Nexus can then configure the canonical adapter, run a private
test frame, verify read/write, and close the blocker automatically.
