# WP9K Oracle Context Evidence

Oracle profile `nova_nexus`, Hermes `0.20.6`, reached the Mac canonical read
layer over the private tunnel. The `nexus_get_reviews` response was real,
current, and timestamped: zero active review items required Ray attention at
the observed time. The system-health response preserved `CURRENT`/`PARTIAL`
provenance rather than converting incomplete telemetry into a healthy claim.

The current-state path is narrow and structured; no report dump or broad
filesystem access is injected into the Oracle prompt.

`NOVA_CURRENT_COMPANY_CONTEXT=PASS_REAL` and
`NOVA_CURRENT_STATE_QUERY=PASS_REAL` for the exercised read path.
