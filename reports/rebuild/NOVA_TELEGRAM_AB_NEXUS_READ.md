# Nova Telegram A/B Nexus read

The shadow remains permitted to perform only the previously proven bounded
Nexus reads. It cannot mutate Nexus, send Telegram, publish, spend, or change
production data. The A/B receipt schema records Nexus resource selection and
returned result metadata separately from the custom path.

`REAL_TELEGRAM_AB_NEXUS_READ=NOT_RUN`; no claim is made until Ray exercises the
existing bot with the certification prompt.
