# Nova Telegram A/B multi-resource

The shadow can run native web/Nexus/Alpha reads in one Hermes context and the
fan-out persists the complete shadow tool sequence. Only the custom response is
sent to Telegram. The A/B hook does not add a brain layer or a pre-model router.

`REAL_TELEGRAM_AB_MULTI_RESOURCE=NOT_RUN`; development evidence is not being
relabelled as real Telegram evidence.
