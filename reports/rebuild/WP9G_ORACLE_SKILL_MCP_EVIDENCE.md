# WP9G Oracle skill/MCP evidence

Hermes 0.20.6 real CLI help exposed native `skills`, `tools`, `mcp`, `browser`,
`sessions`, and `gateway` surfaces. The existing container logs also confirm
82 bundled skills synchronized and the browser binary available.

This proves discovery surface availability only. A real skill selection and a
safe MCP read through a successful model turn were not proven because model
execution timed out. No new MCP server was added.
