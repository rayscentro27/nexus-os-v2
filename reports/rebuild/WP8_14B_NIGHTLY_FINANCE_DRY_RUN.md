# WP8.14B Nightly Finance dry run

The bounded local dry run processed five internal work orders. Every job produced a Finance preflight and postrun receipt; all decisions were `ALLOW` at zero cash use, all completed with zero cash/resource consumption, and publication/payment/live trading remained false. The resulting ledger and rollup are in `public/runtime/finance-preflight.json`.
