# Open-Source Research and Adoption

Pipeline: discovery → license/security/dependency/platform/overlap/cost audit
→ concept or code-pattern classification → sandbox → benchmark → authority
check → recommendation. GitHub authority is `READ_RESEARCH`; autonomous
production installation authority is `NONE`. Concept-only adoption is
supported without installing a repository.

