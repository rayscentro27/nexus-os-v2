# Unattended Runtime Proof

`nexus_oanda_practice_engine.py --daemon --max-cycles N` runs without Codex/OpenCode and exits after the bounded window. A one-cycle real Practice run completed with `WAITING_FOR_VALID_SIGNAL`, fresh market/reconciliation reads, and no manual order. Multiple unattended strategy trades remain pending forward observation.
