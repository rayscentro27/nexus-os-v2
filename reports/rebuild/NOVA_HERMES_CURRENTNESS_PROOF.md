# Nova Hermes currentness proof

Current-world prompts selected the shadow public-web tool and performed real
searches. Search results contain provider timestamps, but the Bing HTML fallback
does not reliably publish an authoritative publication date for every result.
Therefore source dates are recorded as `UNKNOWN` unless the retrieved page
itself supplies one. “Current” is not inferred solely from a search timestamp.

Tesla probe: real search and two bounded HTTP retrieval calls occurred before
the final answer. The answer separated verified search/page observations from
Nova's interpretation. Source-date quality remains partial where pages did not
expose a clear publication date.
