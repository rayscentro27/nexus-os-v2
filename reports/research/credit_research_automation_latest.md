# Credit Research Automation

The ingester hashes/deduplicates summarized sources, separates claims, flags promotion, and defaults practitioner content to `needs_verification`. The fixture stores no transcript.

The bounded queue processes one item with `--once` or up to ten with `--max-items`. It never auto-approves. Research approval counts appear in the existing admin workbench; a dashboard redesign is deferred.
