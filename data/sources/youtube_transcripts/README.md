# Approved YouTube transcript intake

Place user-provided, legally usable `.txt` transcripts here. The filename must contain an approved target ID/handle, or add a matching `.txt.approved` sidecar. Nexus never downloads video or audio from this folder workflow.

Run:

```bash
python3 scripts/activation/run_youtube_transcript_import.py --json
```
