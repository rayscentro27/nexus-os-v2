# secrets — never commit real values
.env
.env.*
!.env.example

# deps / build
node_modules/
dist/
build/

# runtime junk (NOT system state — Supabase is the source of truth)
logs/
outputs/
reports/runtime/
reports/nova_study/
receipts/
secrets/
*.key
.stripe_test_runtime/
.oanda_demo_runtime/

# python / misc
__pycache__/
.venv/
.DS_Store
supabase/.temp/

# UI backup folders (local only)
.nexus_ui_backup_*/
.nexus_backups/
_incoming/

# Local Netlify folder
.netlify

# Temporal local persistence
.temporal/

data/private/
data/runtime/execution_telemetry/
data/runtime/nexus_operational_truth.db*
data/governed/

# private voice runtime/model/audio (never commit binaries or recordings)
tools/voice/runtime/
tools/voice/models/
tools/voice/audio/
