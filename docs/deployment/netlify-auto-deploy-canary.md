# Netlify automatic deploy canary

This documentation-only marker verifies the temporary GitHub `main` →
Netlify production deployment path for site `nexusv20`.

It carries no runtime behavior and does not change the Client Portal V2
cutover policy.
