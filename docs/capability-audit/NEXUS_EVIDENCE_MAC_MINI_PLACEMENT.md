# Mac Mini Placement Result

MarkItDown placement: `GOOD` for bounded local conversion. The successful fixture pilots were short, isolated, and did not alter the certified schedulers.

Crawl4AI placement: `ACCEPTABLE_FOR_PILOT_ONLY` as a contract/safety implementation, but not live-certified on the current macOS 12.7.6 host because its Playwright Chromium dependency cannot launch. A future browser-capable environment or remote CPU worker should be evaluated before enabling live web acquisition.

Neither adapter belongs in the Continuous Loop, Active Operator, Recovery Check, Hermes polling loop, or Mission Control producer. Heavy or browser-dependent work must remain isolated and optional.
